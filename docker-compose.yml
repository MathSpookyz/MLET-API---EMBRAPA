services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: MLET-API---EMBRAPA
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1